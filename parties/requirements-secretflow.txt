# PHYLAX · SecretFlow SPU backend dependencies
# =============================================================================
# SecretFlow supports LINUX + Python 3.10/3.11 ONLY. This file is used ONLY by
# Dockerfile.secretflow — never by Dockerfile.party / Dockerfile.coordinator.
#
# NOT installed or built in this environment (no docker daemon). We pin
# `secretflow` here to assert the exact version parties/common/psi_secretflow.py
# targets — its SPU device provides the ECDH_PSI_3PC / ECDH_PSI_2PC protocols.
# 1.10.0b1 is the newest release on PyPI (SecretFlow ships beta releases only).
#
# NOTE: numpy is intentionally NOT re-pinned here — SecretFlow owns that
# constraint. If pip reports a conflict between SecretFlow's transitive pins and
# fastapi/uvicorn/pydantic below, relax the web-layer pins (they are independent
# of SecretFlow's numeric stack), not SecretFlow.

secretflow==1.10.0b1

# web layer for coordinator_service (kept in sync with requirements.txt)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
